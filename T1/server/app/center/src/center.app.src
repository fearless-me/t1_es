{application, center,
    [
        {description, ""},
        {vsn, "1"},
        {registered, []},
        {applications, [kernel, stdlib, mysql_poolboy, mysql, poolboy, econfig, dynamic_compile, recon]},
        {mod, {center, []}},
        {env, [{mode, dist}]}
    ]}.
